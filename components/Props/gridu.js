import { Box, Grid } from "@chakra-ui/react"

export default function Gridu() {
    return (
        <Box display="grid" gridGap={2} gridAutoFlow="row dense">
            Grid 
        </Box>
    );
}
